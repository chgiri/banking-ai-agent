meta {
  name: 04 - Confirm Transfer
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/banking-actions/chat
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "accountId": "ACC1001",
    "conversationId": "bruno-banking-1",
    "message": "Confirm CONF-REPLACE-WITH-REAL-CODE"
  }
}

docs {
  Replace the confirmation code in the message with the real one returned
  by "03 - Propose Transfer". Only THEN should the transfer actually execute.
}
