services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: bankingfaq
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - bfa-postgres-data:/var/lib/postgresql/data
volumes:
  bfa-postgres-data: